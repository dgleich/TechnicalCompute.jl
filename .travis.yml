# Documentation: http://docs.travis-ci.com/user/languages/julia/
language: julia
os:
  - linux
  - osx
  - windows
julia:
  - 1.0
notifications:
  email: false
script:
 - julia --color=yes -e 'using Pkg; Pkg.build()'
 - travis_wait 40 julia --check-bounds=no --color=yes -e 'using Pkg; Pkg.test()'
